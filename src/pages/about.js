import React from "react";
import "../styles/about.css"; // Import a separate CSS file for the About component

const About = () => {
    return (
        <div className="about-container">
            <h1 className="about-heading">
            </h1>
        </div>
    );
};

export default About;
